const procResult = await tx.run(
  `CALL "GENERIC_DATA_VALIDATOR"(?, ?, ?, ?, ?, ?)`,
  [
    dummyId,
    MODULE_NAME,
    SUB_MODULE_NAME,
    LOAD_TEMPLATE,
    null,
    null
  ]
);

const outParams = procResult[procResult.length - 1][0];
const status = outParams.O_STATUS;
const message = outParams.O_MESSAGE;

if (status === 'E') {

  await tx.run(
    DELETE.from("COM_SCB_FILEUPLOAD_MASTER_DUMMY").where({ ID: dummyId })
  );

  return {
    message: message,
    flag: false
  };
}
