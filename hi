<Panel headerText="MFU Treemap View">

<viz:VizFrame
    id="idTreemap"
    vizType="treemap"
    height="450px"
    vizProperties='{
        "title":{"visible":false},
        "plotArea":{
            "dataLabel":{"visible":true}
        }
    }'>

    <viz:dataset>
        <viz.data:FlattenedDataset data="{/treeData}">

            <viz.data:dimensions>
                <viz.data:DimensionDefinition name="Module" value="{module}"/>
                <viz.data:DimensionDefinition name="Submodule" value="{submodule}"/>
                <viz.data:DimensionDefinition name="Template" value="{template}"/>
            </viz.data:dimensions>

            <viz.data:measures>
                <viz.data:MeasureDefinition name="Count" value="{value}"/>
            </viz.data:measures>

        </viz.data:FlattenedDataset>
    </viz:dataset>

    <viz:feeds>

        <!-- HIERARCHY -->
        <viz.feeds:FeedItem
            uid="categoryAxis"
            type="Dimension"
            values="Module,Submodule,Template"/>

        <!-- SIZE -->
        <viz.feeds:FeedItem
            uid="size"
            type="Measure"
            values="Count"/>

        <!-- COLOR -->
        <viz.feeds:FeedItem
            uid="color"
            type="Dimension"
            values="Module"/>

    </viz:feeds>

</viz:VizFrame>

</Panel>


sap.ui.define([
    "sap/ui/core/mvc/Controller",
    "sap/ui/model/json/JSONModel"
], function (Controller, JSONModel) {
    "use strict";

    return Controller.extend("mfuanalytics.controller.mfureport", {

        onInit: function () {

            var data = {

                // 🔹 KPI + Bar Chart
                modules: [
                    { module: "NFRP", uploaded: 45, approved: 30, pending: 10 },
                    { module: "SCBI", uploaded: 70, approved: 50, pending: 15 },
                    { module: "ESG", uploaded: 25, approved: 15, pending: 5 },
                    { module: "ICON", uploaded: 50, approved: 35, pending: 7 }
                ],

                pieModules: [],

                // 🔹 Charts
                monthly: [
                    { month: "Jan", value: 10 },
                    { month: "Feb", value: 20 },
                    { month: "Mar", value: 15 }
                ],

                quarterly: [
                    { quarter: "Q1", value: 40 },
                    { quarter: "Q2", value: 25 },
                    { quarter: "Q3", value: 30 }
                ],

                yearly: [
                    { year: "2022", value: 50 },
                    { year: "2023", value: 70 },
                    { year: "2024", value: 60 }
                ],

                // 🔹 Table
                tableData: [
                    { module: "Retail", subModule: "Sales", loadType: "Upload", template: "Temp1", count: 10 },
                    { module: "Finance", subModule: "Accounts", loadType: "Upload", template: "Temp2", count: 25 }
                ],

                // 🔥 TREEMAP FULL HIERARCHY DATA (IMPORTANT)
                treeData: [

                    { module: "NFRP", submodule: "Credit Risk", template: "Exposure_Load", value: 10 },
                    { module: "NFRP", submodule: "Credit Risk", template: "Limit_Load", value: 5 },
                    { module: "NFRP", submodule: "Liquidity", template: "Liquidity_Load", value: 8 },

                    { module: "SCBI", submodule: "Insurance", template: "Policy_Load", value: 12 },
                    { module: "SCBI", submodule: "Insurance", template: "Claim_Load", value: 6 },

                    { module: "ESG", submodule: "Climate", template: "Carbon_Load", value: 7 },
                    { module: "ESG", submodule: "Risk", template: "Risk_Load", value: 4 },

                    { module: "ICON", submodule: "Capital", template: "Capital_Load", value: 9 },
                    { module: "ICON", submodule: "Assets", template: "Asset_Load", value: 6 }

                ],

                // 🔹 KPI values
                totalUploaded: 190,
                totalApproved: 130,
                totalPending: 37,
                totalTemplates: 4
            };

            // Pie uses same modules
            data.pieModules = data.modules;

            // Backup for filter
            this._fullModules = data.modules;

            this.getView().setModel(new JSONModel(data));
        },

        // =========================
        // 🔥 FILTER LOGIC
        // =========================
        onFilterChange: function (oEvent) {

            var selectedKeys = oEvent.getSource().getSelectedKeys();
            var oModel = this.getView().getModel();

            // No selection → reset
            if (selectedKeys.length === 0) {

                oModel.setProperty("/modules", this._fullModules);

                oModel.setProperty("/totalUploaded", 190);
                oModel.setProperty("/totalApproved", 130);
                oModel.setProperty("/totalPending", 37);
                oModel.setProperty("/totalTemplates", 4);

                return;
            }

            // Filter
            var filtered = this._fullModules.filter(item =>
                selectedKeys.includes(item.module)
            );

            // Calculate totals
            var totalUploaded = 0,
                totalApproved = 0,
                totalPending = 0;

            filtered.forEach(item => {
                totalUploaded += item.uploaded;
                totalApproved += item.approved;
                totalPending += item.pending;
            });

            // Update UI
            oModel.setProperty("/modules", filtered);
            oModel.setProperty("/totalUploaded", totalUploaded);
            oModel.setProperty("/totalApproved", totalApproved);
            oModel.setProperty("/totalPending", totalPending);
            oModel.setProperty("/totalTemplates", filtered.length);
        },

        // =========================
        // 🔥 OPTIONAL TREEMAP CLICK
        // =========================
        onTreemapClick: function (oEvent) {
            var data = oEvent.getParameter("data")[0].data;
            console.log("Clicked:", data);
        }

    });
});
