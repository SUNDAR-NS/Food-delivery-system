sap.ui.define([
    "sap/ui/core/mvc/Controller",
    "sap/ui/model/json/JSONModel"
], function (Controller, JSONModel) {
    "use strict";

    return Controller.extend("mfuanalytics.controller.mfureport", {

        onInit: function () {

            var data = {

                allModules: [
                    { module: "NFRP", uploaded: 45, approved: 30, pending: 10 },
                    { module: "SCBI", uploaded: 70, approved: 50, pending: 15 },
                    { module: "ESG", uploaded: 25, approved: 15, pending: 5 },
                    { module: "ICON", uploaded: 50, approved: 35, pending: 7 }
                ],

                modules: [],

                statusList: [
                    { key: "uploaded", text: "Uploaded" },
                    { key: "approved", text: "Approved" },
                    { key: "pending", text: "Pending" }
                ],

                monthly: [
                    { month: "Jan", value: 10 },
                    { month: "Feb", value: 20 },
                    { month: "Mar", value: 15 }
                ],

                quarterly: [
                    { quarter: "Q1", value: 40 },
                    { quarter: "Q2", value: 25 },
                    { quarter: "Q3", value: 30 }
                ],

                yearly: [
                    { year: "2022", value: 50 },
                    { year: "2023", value: 70 },
                    { year: "2024", value: 60 }
                ],

                tableData: [
                    { module: "NFRP", subModule: "Mapping master", loadType: "Upload", template: "Temp1", count: 10 },
                    { module: "SCBI", subModule: "Config master", loadType: "Truncate", template: "Temp2", count: 25 }
                ],

                totalUploaded: 190,
                totalApproved: 130,
                totalPending: 37,
                totalTemplates: 4
            };

            data.modules = data.allModules;

            this._fullModules = data.allModules;

            var oModel = new JSONModel(data);
            this.getView().setModel(oModel);
        },

        // 🔥 FINAL COMBINED FILTER (MODULE + STATUS + CHART FIX)
        onFilterChange: function () {

            var oView = this.getView();
            var oModel = oView.getModel();

            var moduleKeys = oView.byId("moduleFilter").getSelectedKeys();
            var statusKeys = oView.byId("statusFilter").getSelectedKeys();

            var baseData = this._fullModules;

            // 👉 STEP 1: FILTER MODULE
            var filtered = baseData;

            if (moduleKeys.length > 0) {
                filtered = baseData.filter(item =>
                    moduleKeys.includes(item.module)
                );
            }

            // 👉 STEP 2: APPLY STATUS FILTER (IMPORTANT 🔥)
            var finalData = filtered.map(item => {

                return {
                    module: item.module,
                    uploaded: (statusKeys.length === 0 || statusKeys.includes("uploaded")) ? item.uploaded : 0,
                    approved: (statusKeys.length === 0 || statusKeys.includes("approved")) ? item.approved : 0,
                    pending: (statusKeys.length === 0 || statusKeys.includes("pending")) ? item.pending : 0
                };
            });

            // 👉 STEP 3: KPI CALCULATION
            var totalUploaded = 0,
                totalApproved = 0,
                totalPending = 0;

            finalData.forEach(item => {
                totalUploaded += item.uploaded;
                totalApproved += item.approved;
                totalPending += item.pending;
            });

            // 👉 STEP 4: UPDATE MODEL (THIS DRIVES CHART 🔥)
            oModel.setProperty("/modules", finalData);
            oModel.setProperty("/totalUploaded", totalUploaded);
            oModel.setProperty("/totalApproved", totalApproved);
            oModel.setProperty("/totalPending", totalPending);
            oModel.setProperty("/totalTemplates", finalData.length);
        }

    });
});
